import React from 'react';

import { Card, Heading } from '@aws-amplify/ui-react';

const Chat = () => {
  return (
    <Card>
      <Heading level={1}>Chat</Heading>
    </Card>
  );
};

export default Chat;
