import React from 'react';

import { Card, Heading } from '@aws-amplify/ui-react';

const Home = () => {
  return (
    <Card>
      <Heading level={1}>Home</Heading>
    </Card>
  );
};


export default Home;
